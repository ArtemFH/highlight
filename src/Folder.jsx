import './app.css'
import React from 'react';

const Folder = (props) => {
    return (
        <div className={'folder'} id={props.id}>
            <div/>
        </div>
    );
};

export default Folder;